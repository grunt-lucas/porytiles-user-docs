# Installation

```{admonition} Page Status
:class: warning
This page is a placeholder. Content coming soon.
```

Short, procedural installation guide:

- Homebrew installation (primary)
- Download release binaries (Linux, macOS, Windows/WSL)
- Building from source (brief, link to dev docs for details)
- Verifying installation (`porytiles --version`)
- Shell completion setup (`porytiles completion bash/zsh/fish`)

**Cross-references:** {doc}`creating-your-first-tileset` as next step
